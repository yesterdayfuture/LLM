
import torch
import random
import numpy as np

print(torch.empty(1, 0, 4)) 

print(random.choice(range(4)))

print(np.random.choice(np.arange(5), p=[0.1, 0.2, 0.3, 0.4, 0.0]))

print([ i for i in range(4)])

print(np.arange(4))
